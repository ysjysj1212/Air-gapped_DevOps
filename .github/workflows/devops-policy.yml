name: DevOps Policy

on:
  push:
    branches-ignore:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
      - devops
      - feat/setup-project-structure

permissions:
  contents: read

jobs:
  validate:
    name: Validate dependencies, tests, YAML, and commits
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: DevOps

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip
          cache-dependency-path: DevOps/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Check dependency conflicts
        run: pip check

      - name: Run tests
        run: |
          if find tests -name 'test_*.py' -size +0c | grep -q .; then
            pytest tests/
          else
            echo "No non-empty test files found; skipping pytest."
          fi

      - name: Lint YAML files
        run: yamllint .

      - name: Validate branch convention
        working-directory: .
        env:
          PUSH_BRANCH: ${{ github.ref_name }}
          PR_BRANCH: ${{ github.head_ref }}
        run: |
          set -euo pipefail

          if [ "${{ github.event_name }}" = "pull_request" ]; then
            branch="$PR_BRANCH"
          else
            branch="$PUSH_BRANCH"
          fi

          case "$branch" in
            DevOps/*|AI/*)
              ;;
            *)
              echo "Invalid branch name: $branch"
              echo "Use one of:"
              echo "- DevOps/<work-item>"
              echo "- AI/<work-item>"
              exit 1
              ;;
          esac

          work_item="${branch#*/}"
          if [ -z "$work_item" ] || printf '%s\n' "$work_item" | grep -Eq '[[:space:]/]'; then
            echo "Invalid work item segment: $work_item"
            echo "Use a single non-empty segment without spaces or extra slashes."
            exit 1
          fi

      - name: Validate commit convention
        working-directory: .
        env:
          BASE_SHA: ${{ github.event.before }}
          HEAD_SHA: ${{ github.sha }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail

          pattern='^(feat|fix|docs|style|refactor|test|chore)(\([a-z0-9._-]+\))?: .{1,72}$'

          if [ "${{ github.event_name }}" = "pull_request" ]; then
            range="${PR_BASE_SHA}..${PR_HEAD_SHA}"
          else
            if [ "$BASE_SHA" = "0000000000000000000000000000000000000000" ]; then
              if git show-ref --verify --quiet refs/remotes/origin/main; then
                base_branch="origin/main"
              elif git show-ref --verify --quiet refs/remotes/origin/master; then
                base_branch="origin/master"
              else
                base_branch="origin/HEAD"
              fi
              base="$(git merge-base "$base_branch" "$HEAD_SHA")"
              range="${base}..${HEAD_SHA}"
            else
              range="${BASE_SHA}..${HEAD_SHA}"
            fi
          fi

          invalid_messages=""
          while IFS= read -r subject; do
            if ! printf '%s\n' "$subject" | grep -Eq "$pattern"; then
              invalid_messages="${invalid_messages}${subject}\n"
            fi
          done < <(git log --format=%s "$range")

          if [ -n "$invalid_messages" ]; then
            printf 'Invalid commit messages:\n%b\n' "$invalid_messages"
            printf 'Use: type(scope): subject\n'
            printf 'Allowed types: feat, fix, docs, style, refactor, test, chore\n'
            exit 1
          fi
