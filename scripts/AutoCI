#!/bin/sh

set -eu

SCRIPT_REALPATH="$(python3 -c 'import os,sys; print(os.path.realpath(sys.argv[1]))' "$0")"
REPO_ROOT="$(cd "$(dirname "$SCRIPT_REALPATH")/.." && pwd)"

exec python3 "$REPO_ROOT/scripts/llm-cli.py" AutoCI "$@"
